---
// Componente principal de Gommi
import logoGommi from '../assets/images/gommi_logos_logo_full_color.png';
---

<div id="container">
	<header>
		<nav>
			<div class="logo">
				<img src={logoGommi.src} alt="Gommi" class="logo-img" />
			</div>
			<ul class="nav-links">
				<li><a href="#beneficios">Beneficios</a></li>
				<li><a href="#como-funciona">¿Cómo funciona?</a></li>
				<li><a href="#testimonios">Testimonios</a></li>
			</ul>
			<a href="https://www.instagram.com/gommi_oficial/" target="_blank" class="cta-button">Comprar</a>
		</nav>
	</header>

	<main>
		<section id="hero">
			<div class="hero-content">
				<span class="badge">Probióticos + Prebióticos</span>
				<h1>Gomitas Simbióticas</h1>
				<p class="tagline">Bienestar digestivo diario, <strong>delicioso</strong> y <strong>fácil</strong></p>
				<p class="dose">Solo 2 gomitas al día</p>
				<div class="hero-buttons">
					<a href="https://www.instagram.com/gommi_oficial/" target="_blank" class="button primary">Quiero las mías</a>
					<a href="#beneficios" class="button secondary">Conocer más</a>
				</div>
			</div>
			<div class="hero-image">
				<div class="gummy-container">
					<div class="gummy gummy-1"></div>
					<div class="gummy gummy-2"></div>
					<div class="gummy gummy-3"></div>
					<div class="floating-text">Deliciosas</div>
				</div>
			</div>
		</section>

		<section id="beneficios">
			<h2>¿Por qué elegir Gommi?</h2>
			<div class="benefits-grid">
				<div class="benefit-card">
					<div class="benefit-icon">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
						</svg>
					</div>
					<h3>Mejora tu digestión</h3>
					<p>Reduce la inflamación y distensión abdominal de forma natural</p>
				</div>
				<div class="benefit-card">
					<div class="benefit-icon">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
						</svg>
					</div>
					<h3>Fortalece tu sistema inmune</h3>
					<p>Probióticos que ayudan a mantener tus defensas activas</p>
				</div>
				<div class="benefit-card">
					<div class="benefit-icon">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<circle cx="12" cy="12" r="10"/>
							<path d="M8 14s1.5 2 4 2 4-2 4-2"/>
							<line x1="9" y1="9" x2="9.01" y2="9"/>
							<line x1="15" y1="9" x2="15.01" y2="9"/>
						</svg>
					</div>
					<h3>Sabor delicioso</h3>
					<p>Una gomita que realmente querrás tomar todos los días</p>
				</div>
				<div class="benefit-card">
					<div class="benefit-icon">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/>
						</svg>
					</div>
					<h3>Fácil de consumir</h3>
					<p>Sin agua, sin complicaciones. En cualquier momento y lugar</p>
				</div>
			</div>
		</section>

		<section id="como-funciona">
			<h2>¿Cómo funciona?</h2>
			<div class="steps">
				<div class="step">
					<div class="step-number">1</div>
					<h3>Toma 2 gomitas</h3>
					<p>Cada día, a la hora que prefieras</p>
				</div>
				<div class="step">
					<div class="step-number">2</div>
					<h3>Los probióticos actúan</h3>
					<p>Equilibran tu flora intestinal</p>
				</div>
				<div class="step">
					<div class="step-number">3</div>
					<h3>Siente la diferencia</h3>
					<p>Menos inflamación, más bienestar</p>
				</div>
			</div>
		</section>

		<section id="testimonios">
			<h2>Lo que dicen quienes ya probaron Gommi</h2>
			<div class="testimonials-grid">
				<div class="testimonial-card">
					<p>"Resultados reales. Personas reales. Bienestar digestivo que se siente."</p>
					<span class="author">- Cliente satisfecha</span>
				</div>
				<div class="testimonial-card">
					<p>"Por fin un suplemento que realmente quiero tomar. ¡Saben increíble!"</p>
					<span class="author">- Usuario verificado</span>
				</div>
			</div>
		</section>

		<section id="cta-final">
			<div class="cta-box">
				<h2>¿Lista para sentirte mejor?</h2>
				<p>Un ritual digestivo pensado para todos</p>
				<a href="https://www.instagram.com/gommi_oficial/" target="_blank" class="button primary large">Quiero mis Gommi</a>
			</div>
		</section>
	</main>

	<footer>
		<div class="footer-content">
			<div class="footer-logo">
				<img src={logoGommi.src} alt="Gommi" class="footer-logo-img" />
			</div>
			<p>Gomitas simbióticas - Bienestar digestivo diario</p>
			<div class="social-links">
				<a href="https://www.instagram.com/gommi_oficial/" target="_blank" aria-label="Instagram">
					<svg viewBox="0 0 24 24" fill="currentColor">
						<path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
					</svg>
				</a>
			</div>
			<p class="copyright">© 2024 Gommi. Todos los derechos reservados.</p>
		</div>
	</footer>
</div>

<style>
	/* Variables de color */
	:root {
		--primary: #E91E8C;
		--primary-light: #FFE4EC;
		--primary-dark: #C4177A;
		--secondary: #4ECDC4;
		--secondary-light: #B8F0EC;
		--text: #2D3748;
		--text-light: #718096;
		--white: #FFFFFF;
	}

	/* Reset y base */
	* {
		box-sizing: border-box;
	}

	#container {
		font-family: 'Nunito', sans-serif;
		color: var(--text);
		overflow-x: hidden;
	}

	/* Header y navegación */
	header {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		z-index: 100;
		background: rgba(255, 255, 255, 0.95);
		backdrop-filter: blur(10px);
		box-shadow: 0 2px 20px rgba(233, 30, 140, 0.1);
	}

	nav {
		max-width: 1200px;
		margin: 0 auto;
		padding: 1rem 2rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.logo {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		font-size: 1.5rem;
		font-weight: 800;
		color: var(--primary);
		text-decoration: none;
	}

	.logo-img {
		height: 50px;
		width: auto;
	}

	.nav-links {
		display: flex;
		list-style: none;
		gap: 2rem;
		margin: 0;
		padding: 0;
	}

	.nav-links a {
		text-decoration: none;
		color: var(--text);
		font-weight: 600;
		transition: color 0.3s;
	}

	.nav-links a:hover {
		color: var(--primary);
	}

	.cta-button {
		background: var(--primary);
		color: var(--white);
		padding: 0.75rem 1.5rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 700;
		transition: all 0.3s;
	}

	.cta-button:hover {
		background: var(--primary-dark);
		transform: translateY(-2px);
		box-shadow: 0 4px 15px rgba(233, 30, 140, 0.4);
	}

	/* Main */
	main {
		padding-top: 80px;
	}

	/* Hero Section */
	#hero {
		min-height: calc(100vh - 80px);
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 2rem;
		max-width: 1200px;
		margin: 0 auto;
		gap: 4rem;
	}

	.hero-content {
		flex: 1;
	}

	.badge {
		display: inline-block;
		background: var(--secondary);
		color: var(--white);
		padding: 0.5rem 1rem;
		border-radius: 50px;
		font-size: 0.875rem;
		font-weight: 700;
		margin-bottom: 1rem;
	}

	.hero-content h1 {
		font-size: 3.5rem;
		font-weight: 800;
		color: var(--primary);
		margin: 0 0 1rem;
		line-height: 1.1;
	}

	.tagline {
		font-size: 1.5rem;
		color: var(--text);
		margin: 0 0 0.5rem;
	}

	.tagline strong {
		color: var(--primary);
	}

	.dose {
		font-size: 1.125rem;
		color: var(--secondary);
		font-weight: 700;
		margin: 0 0 2rem;
	}

	.hero-buttons {
		display: flex;
		gap: 1rem;
	}

	.button {
		display: inline-block;
		padding: 1rem 2rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 700;
		font-size: 1rem;
		transition: all 0.3s;
	}

	.button.primary {
		background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
		color: var(--white);
		box-shadow: 0 4px 15px rgba(233, 30, 140, 0.3);
	}

	.button.primary:hover {
		transform: translateY(-3px);
		box-shadow: 0 8px 25px rgba(233, 30, 140, 0.4);
	}

	.button.secondary {
		background: var(--white);
		color: var(--primary);
		border: 2px solid var(--primary);
	}

	.button.secondary:hover {
		background: var(--primary-light);
	}

	.button.large {
		padding: 1.25rem 2.5rem;
		font-size: 1.125rem;
	}

	/* Hero Image / Gummies */
	.hero-image {
		flex: 1;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.gummy-container {
		position: relative;
		width: 300px;
		height: 300px;
	}

	.gummy {
		position: absolute;
		width: 80px;
		height: 80px;
		border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
		animation: float 3s ease-in-out infinite;
	}

	.gummy-1 {
		background: linear-gradient(135deg, #FF6B9D 0%, #E91E8C 100%);
		top: 20%;
		left: 20%;
		animation-delay: 0s;
		box-shadow: 0 10px 30px rgba(233, 30, 140, 0.3), inset 0 -10px 20px rgba(0,0,0,0.1), inset 0 10px 20px rgba(255,255,255,0.3);
	}

	.gummy-2 {
		background: linear-gradient(135deg, #7FDBDA 0%, #4ECDC4 100%);
		top: 40%;
		right: 15%;
		animation-delay: 0.5s;
		box-shadow: 0 10px 30px rgba(78, 205, 196, 0.3), inset 0 -10px 20px rgba(0,0,0,0.1), inset 0 10px 20px rgba(255,255,255,0.3);
	}

	.gummy-3 {
		background: linear-gradient(135deg, #FFB6D3 0%, #FF6B9D 100%);
		bottom: 20%;
		left: 35%;
		animation-delay: 1s;
		box-shadow: 0 10px 30px rgba(255, 107, 157, 0.3), inset 0 -10px 20px rgba(0,0,0,0.1), inset 0 10px 20px rgba(255,255,255,0.3);
	}

	.floating-text {
		position: absolute;
		bottom: 5%;
		right: 10%;
		background: var(--white);
		color: var(--primary);
		padding: 0.5rem 1rem;
		border-radius: 20px;
		font-weight: 700;
		font-size: 0.875rem;
		box-shadow: 0 4px 15px rgba(0,0,0,0.1);
		animation: float 4s ease-in-out infinite;
	}

	@keyframes float {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-20px); }
	}

	/* Benefits Section */
	#beneficios {
		padding: 6rem 2rem;
		background: var(--white);
	}

	#beneficios h2, #como-funciona h2, #testimonios h2 {
		text-align: center;
		font-size: 2.5rem;
		color: var(--text);
		margin-bottom: 3rem;
	}

	.benefits-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		max-width: 1200px;
		margin: 0 auto;
	}

	.benefit-card {
		background: var(--primary-light);
		padding: 2rem;
		border-radius: 20px;
		text-align: center;
		transition: transform 0.3s, box-shadow 0.3s;
	}

	.benefit-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(233, 30, 140, 0.15);
	}

	.benefit-icon {
		width: 60px;
		height: 60px;
		background: var(--primary);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		margin: 0 auto 1rem;
	}

	.benefit-icon svg {
		width: 30px;
		height: 30px;
		color: var(--white);
	}

	.benefit-card h3 {
		color: var(--primary);
		margin: 0 0 0.5rem;
		font-size: 1.25rem;
	}

	.benefit-card p {
		color: var(--text-light);
		margin: 0;
		line-height: 1.6;
	}

	/* How it works */
	#como-funciona {
		padding: 6rem 2rem;
		background: linear-gradient(135deg, var(--secondary-light) 0%, var(--white) 100%);
	}

	.steps {
		display: flex;
		justify-content: center;
		gap: 3rem;
		max-width: 900px;
		margin: 0 auto;
		flex-wrap: wrap;
	}

	.step {
		text-align: center;
		flex: 1;
		min-width: 200px;
	}

	.step-number {
		width: 60px;
		height: 60px;
		background: var(--secondary);
		color: var(--white);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 1.5rem;
		font-weight: 800;
		margin: 0 auto 1rem;
	}

	.step h3 {
		color: var(--text);
		margin: 0 0 0.5rem;
	}

	.step p {
		color: var(--text-light);
		margin: 0;
	}

	/* Testimonials */
	#testimonios {
		padding: 6rem 2rem;
		background: var(--white);
	}

	.testimonials-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		max-width: 800px;
		margin: 0 auto;
	}

	.testimonial-card {
		background: linear-gradient(135deg, var(--primary-light) 0%, var(--white) 100%);
		padding: 2rem;
		border-radius: 20px;
		border-left: 4px solid var(--primary);
	}

	.testimonial-card p {
		font-style: italic;
		color: var(--text);
		margin: 0 0 1rem;
		font-size: 1.125rem;
		line-height: 1.6;
	}

	.testimonial-card .author {
		color: var(--primary);
		font-weight: 700;
	}

	/* CTA Final */
	#cta-final {
		padding: 6rem 2rem;
		background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
	}

	.cta-box {
		text-align: center;
		max-width: 600px;
		margin: 0 auto;
	}

	.cta-box h2 {
		color: var(--white);
		font-size: 2.5rem;
		margin: 0 0 1rem;
	}

	.cta-box p {
		color: var(--primary-light);
		font-size: 1.25rem;
		margin: 0 0 2rem;
	}

	.cta-box .button.primary {
		background: var(--white);
		color: var(--primary);
	}

	.cta-box .button.primary:hover {
		background: var(--primary-light);
		box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
	}

	/* Footer */
	footer {
		background: var(--text);
		color: var(--white);
		padding: 3rem 2rem;
	}

	.footer-content {
		max-width: 1200px;
		margin: 0 auto;
		text-align: center;
	}

	.footer-logo {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
		font-size: 1.25rem;
		font-weight: 800;
		margin-bottom: 1rem;
	}

	.footer-logo-img {
		height: 40px;
		width: auto;
	}

	.footer-content > p {
		color: var(--text-light);
		margin: 0 0 1.5rem;
	}

	.social-links {
		display: flex;
		justify-content: center;
		gap: 1rem;
		margin-bottom: 1.5rem;
	}

	.social-links a {
		width: 40px;
		height: 40px;
		background: var(--primary);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--white);
		transition: all 0.3s;
	}

	.social-links a:hover {
		background: var(--primary-dark);
		transform: translateY(-3px);
	}

	.social-links svg {
		width: 20px;
		height: 20px;
	}

	.copyright {
		font-size: 0.875rem;
		color: var(--text-light);
	}

	/* Responsive */
	@media screen and (max-width: 768px) {
		.nav-links {
			display: none;
		}

		#hero {
			flex-direction: column;
			text-align: center;
			padding-top: 4rem;
		}

		.hero-content h1 {
			font-size: 2.5rem;
		}

		.hero-buttons {
			justify-content: center;
			flex-wrap: wrap;
		}

		.gummy-container {
			width: 250px;
			height: 250px;
		}

		.gummy {
			width: 60px;
			height: 60px;
		}

		#beneficios h2, #como-funciona h2, #testimonios h2 {
			font-size: 2rem;
		}

		.steps {
			flex-direction: column;
			align-items: center;
		}

		.cta-box h2 {
			font-size: 2rem;
		}
	}
</style>
