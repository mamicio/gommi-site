services:
  - type: web
    name: gommi-site
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
